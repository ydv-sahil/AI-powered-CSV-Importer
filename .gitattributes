# Normalize to LF in the repository and in the working tree.
# Without this, a Windows checkout commits CRLF and every file shows as modified
# for anyone reviewing on Linux or macOS.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
